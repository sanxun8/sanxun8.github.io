---
tilte: 7.1 jQuery中的迭代器
---

迭代器模式无非就是循环访问聚合对象中的各个元素。比如 jQuery 中的$.each 函数，其中回调函数中的参数 i 为当前索引，n 为当前元素，代码如下：

```javascript
$.each([1, 2, 3], function (i, n) {
  console.log("当前下标为： " + i);
  console.log("当前值为：" + n);
});
```
