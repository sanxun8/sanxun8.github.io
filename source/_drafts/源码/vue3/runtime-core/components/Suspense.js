export function queueEffectWithSuspense(fn, suspense) {
  if (suspense && suspense.pedingBranch) {
    if (isArray(fn)) {
      suspense.effects.push(...fn);
    } else {
      suspense.effects.push(fn);
    }
  } else {
    queuePostFlushCb(fn);
  }
}
